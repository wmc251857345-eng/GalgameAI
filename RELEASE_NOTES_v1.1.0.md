# GALA v1.1.0 更新说明

> 2026-08-26 · 构建 9d799eb（含 v0.3.0 全部内容）
> 升级方法：下载压缩包解压，用新文件覆盖旧版的 `GalgameAI.exe`、`_internal/`、`frontend/`、`version.json` 即可。
> **数据库、封面、存档备份、配置全在 exe 旁边，不会被覆盖，老库自动无损迁移到新 schema。**

## ✨ 新功能

- ⭐ **我的评分**：详情页点星星打 1~5 分（再点一下清除），卡片/列表显示星标，库页新增「我的评分」排序——自己的分和 VNDB 外部分开看
- 📝 **个人笔记**：详情页编辑里新增"我的笔记"，攻略进度/汉化组/感想随手记；全局搜索能搜到笔记和简介
- 🎯 **想玩清单**：侧栏新增「想玩清单」——还没入库的种草游戏先记下来，支持备注；厂商墙的作品弹层里可一键「加入想玩」
- ☑️ **批量操作**：库页新增「批量」模式，多选后一键设游玩进度（进行中/通关/未开始）、批量加标签、批量删除
- 🔄 **更新检查**：启动时静默检查 GitHub Releases（24h 缓存），有新版侧栏出现提示条；设置页 → 关于与更新 可手动检查
- 🔍 **搜索增强**：搜索现在覆盖简介和个人笔记

## 🩸 重要修复

- **误启动幽灵时长**：秒开秒关的误启动会留下未结算会话，下次启动被补记最长 **24 小时**幽灵游玩时长——现在误启动立即按 0 秒结算
- **升级不再重建数据库**：旧版本号变化时会清空重建库；v1.1 起全部走无损迁移（只加列不动数据），升级前仍自动备份一份旧库双保险
- 删除游戏现在同步清理存档备份元数据；配置存档路径不再清零备份统计
- 游戏运行中禁止恢复存档；备份目标指向未接入的 U 盘/移动盘不再崩溃
- 启动参数多参数正确拆分传递；二次裁剪封面不再裁错区域
- 一批 UI 卡死防御：扫描进度轮询、翻译状态轮询、设置页加载等失败不再永久卡死；约 40 个前端桥接调用补齐超时保护
- 厂商档案点已拥有作品、"打开本地游戏"按钮失效已修复

## ❌ 移除

- **Locale Emulator 集成**：实践验证不可靠（LEProc 只是启动器、秒退且依赖每游戏配置文件）。需要转区请直接用 LE 的右键菜单启动游戏，GALA 照常记录时长与备份存档。

## 📦 校验

- 版本自检：侧栏底部应显示 `v1.1.0 · 2026-08-26`
